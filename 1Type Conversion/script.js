try {
    function converToNumber(str) {
        if (num = Number(str)) {
            console.log(num);
        } else {
            console.log("Invalid number");
        }
    }
    converToNumber("bgj");
} catch (error) {
    console.log(error);
}
